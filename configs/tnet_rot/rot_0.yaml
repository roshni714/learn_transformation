data_loader:
  name: "CIFAR10"
  corruption:
    degrees: 0
device: "cuda:0"
batch_size: 128
tnet:
  name: "vec"
  save_as: "runs/rot_0"
  save_dir: "tnet_models/rot_0"
  transform_list: ["rotation"]
  batch_size: 128
pretrained_model:
  path: "pretrained_models/cifar_resize_randomcrop/checkpoint_17.tar"

